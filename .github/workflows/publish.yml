name: Publish to NPM

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run @atp/spec tests
        run: cd packages/spec && npm test

      - name: Publish @atp/spec to NPM
        run: npm publish --workspace @atp/spec
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub release summary
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.releases.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: context.payload.release.id,
              body: context.payload.release.body + '\n\n## Published\n- `@atp/spec@' + context.payload.release.tag_name.replace(/^v/, '') + '` published to NPM'
            });
